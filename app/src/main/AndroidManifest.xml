<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masudin.omahkamerasragen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Penyewaan_kamera">
        <activity android:name=".ui.camera.CameraEditActivity"></activity>
        <activity android:name=".ui.camera.CameraDetailActivity" />
        <activity android:name=".ui.product.ProductEditActivity" />
        <activity android:name=".ui.product.ProductDetailActivity" />
        <activity android:name=".ui.product.ProductAddActivity" />
        <activity android:name=".ui.camera.CameraAddActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.product.ProductActivity" />
        <activity android:name=".ui.history_transaction.HistoryTransactionActivity" />
        <activity android:name=".ui.denda.DendaActivity" />
        <activity android:name=".ui.cart.CartActivity" />
        <activity android:name=".ui.camera.CameraActivity" />
        <activity android:name=".HomepageActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>